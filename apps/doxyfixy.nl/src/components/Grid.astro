<div class="grid">
    <slot/>
</div>

<style>
    .grid {
        display: grid;
        width: 100%;
        max-width: 1280px;
        grid-template-columns: repeat(auto-fit, minmax(calc(max(160px, 20%)), 1fr));
        gap: var(--doxy-padding-xl);
        margin: 0 auto;
    }
</style>